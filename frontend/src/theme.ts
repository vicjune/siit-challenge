import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: 'rgb(80, 79, 243)',
    },
    secondary: {
      main: '#fff',
    },
    background: {
      default: 'rgba(231, 231, 241, 1)',
    },
  },
});
